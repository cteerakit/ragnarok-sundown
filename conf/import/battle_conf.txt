area_size: 25
max_walk_path: 25

// conf/battle/client.conf
// Don't show rAthena version/Git Hash to players on login
display_version: no

// conf/battle/player.conf
// Players respawn with full HP/SP (percentage of max)
restart_hp_rate: 100
restart_sp_rate: 100

// conf/battle/drops.conf
// 5x drop rate across the board (100 = official 1x). Logarithmic scaling
// (item_logarithmic_drops) keeps rare drops from approaching 100%.
// Normal monsters
item_rate_common: 500
item_rate_equip: 500
item_rate_card: 500
item_rate_heal: 500
item_rate_use: 500
// Boss-class monsters
item_rate_common_boss: 500
item_rate_equip_boss: 500
item_rate_card_boss: 500
item_rate_heal_boss: 500
item_rate_use_boss: 500
// MVP drops
item_rate_common_mvp: 500
item_rate_equip_mvp: 500
item_rate_card_mvp: 500
item_rate_heal_mvp: 500
item_rate_use_mvp: 500
item_rate_mvp: 500
// Treasure boxes
item_rate_treasure: 500
// Scale drop rates logarithmically so high multipliers don't push rare
// drops to near-100%; tames the effect of our boosted item_rate_* values.
item_logarithmic_drops: yes

// conf/battle/party.conf
// Encourage partying: even-share bonus of 100% per extra member means the
// party's total EXP scales to N*100%, so each member receives a full 100%.
party_even_share_bonus: 100

// conf/battle/exp.conf
// Mid-rate multiplier (2000 = 20x). Stacks with the flattened curve in
// db/re/job_exp.yml (see tools/exp_curve.py).
base_exp_rate: 2000
job_exp_rate: 2000
mvp_exp_rate: 2000
quest_exp_rate: 2000
// Extra EXP per additional attacker on a monster (default 25).
exp_bonus_attacker: 100